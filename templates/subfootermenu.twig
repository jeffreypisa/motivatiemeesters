{% if subfootermenu %}
	<ul class="menuextra">
    <li> ©{{ "now"|date("Y") }} Motivatiemeesters </li>
  	{% for item in subfootermenu %}
  		<li class="{{item.classes | join(' ')}}">
  			<a href="{{item.get_link}}">{{item.title}}</a>
  			{% include "subfootermenu.twig" with {'subfootermenu': item.get_children} %}
  		</li>
  	{% endfor %}
	</ul>
{% endif %}