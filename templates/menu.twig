{% if menu %}
	<ul class="menu">
  	{% for item in menu %}
  		<li class="{{item.classes | join(' ')}} ">
  			<a href="{{item.get_link}}"><h2>{{item.title}}</h2></a>
  			{% include "menu.twig" with {'menu': item.get_children} %}
  		</li>
  	{% endfor %}
	</ul>
{% endif %}